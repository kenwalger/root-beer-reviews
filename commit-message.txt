Performance optimizations and code quality improvements

This PR addresses performance issues and code quality concerns identified in the previous PR review.

Key Changes:
- **Performance Optimization**: Eliminated redundant database query in homepage route
  - Reuse `all_rootbeers` data for filter dropdowns instead of fetching again
  - Moved brands/regions extraction before review processing loop
  - Reduces database round-trips and improves page load time
- **Code Quality**: Fixed formatting and removed duplication
  - Fixed indentation inconsistency in template response formatting
  - Removed duplicate JavaScript function in pagination template
  - Consolidated pagination template logic to eliminate code duplication
  - JavaScript function now defined once outside conditional blocks

Performance Impact:
- Eliminates one unnecessary database query per homepage load
- Reduces database load and improves response time
- No functional changes, purely performance and code quality improvements

Files Changed:
- `app/routes/public.py`: Optimized to reuse existing data for filters
- `app/templates/partials/pagination.html`: Removed duplicate JavaScript code

